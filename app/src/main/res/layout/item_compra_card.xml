<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_list"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="0dp"
    android:layout_marginVertical="4dp"
    card_view:cardCornerRadius="0dp"
    card_view:cardElevation="8dp"
    card_view:cardUseCompatPadding="false"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:padding="8dp">

    <!-- Fondo degradado -->
    <LinearLayout
        android:id="@+id/containerGradient"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_card_default"
        android:orientation="vertical"
        android:paddingVertical="6dp"
        android:paddingHorizontal="10dp">

        <!-- 1ra fila: Cantidad + Unidad + Título -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/textViewCardCompraQuantity"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="@android:color/white" />

            <TextView
                android:id="@+id/textViewCardCompraUnit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:text=""
                android:textSize="16sp"
                android:textColor="@android:color/white" />

            <TextView
                android:id="@+id/textViewCardCompraTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="12dp"
                android:text=""
                android:textStyle="bold"
                android:textSize="17sp"
                android:textColor="@android:color/white"
                android:ellipsize="end"
                android:singleLine="true" />
        </LinearLayout>

        <!-- 2da fila: Descripción -->
        <TextView
            android:id="@+id/textViewCardCompraDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="14sp"
            android:textColor="#F0F0F0"
            android:layout_marginTop="2dp"
            android:maxLines="2"
            android:ellipsize="end" />

        <!-- 3ra fila: Icono (izq) + Estado (der) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="2dp"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/iconMessage"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_message"
                android:contentDescription="Tiene notas"
                card_view:tint="@android:color/white"
                android:visibility="visible"
                />

            <View
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/textViewCardCompraComplete"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pendiente"
                android:textColor="@android:color/white"
                android:background="@drawable/bg_type_chip"
                android:paddingHorizontal="6dp"
                android:paddingVertical="1dp"
                android:textSize="10sp"
                android:visibility="visible"
                />
        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
