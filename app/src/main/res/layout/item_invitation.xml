<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="0dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="2dp"
    app:strokeWidth="1dp"
    app:strokeColor="#E0E0E0"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="10dp"
        android:gravity="center_vertical">

        <!-- Imagen del usuario -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/imgFromUserPhoto"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_profile"
            android:layout_marginEnd="10dp"
            app:shapeAppearanceOverlay="@style/CircularImageView"
            app:strokeColor="#E0E0E0"
            app:strokeWidth="1dp" />

        <!-- Contenido textual -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Título de la lista -->
            <TextView
                android:id="@+id/tvListTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Lista de Compras 🛒"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@android:color/black"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Descripción -->
            <TextView
                android:id="@+id/tvListDescription"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Compra semanal para la casa"
                android:textSize="13sp"
                android:textColor="@android:color/darker_gray"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginTop="1dp" />

            <!-- Nombre del usuario -->
            <TextView
                android:id="@+id/tvFromUserName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Federico González"
                android:textSize="14sp"
                android:textColor="#333333"
                android:layout_marginTop="4dp"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Email del usuario -->
            <TextView
                android:id="@+id/tvFromUserEmail"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="alambratore@gmail.com"
                android:textSize="12sp"
                android:textColor="#777777"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <!-- Ícono opcional de estado -->
        <ImageView
            android:id="@+id/imgStatus"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_list"
            android:layout_marginStart="6dp"
            app:tint="#888888" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
